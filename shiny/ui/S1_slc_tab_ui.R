#-----------------------------------------------------------------------------
# S1 Tab
tabItem(tabName = "s1_slc",
        fluidRow(
          #----------------------------------------------------------------------------------
          # Processing Panel Sentinel-1
          box(
            # Title                     
            title = "Sentinel-1 SLC processors", status = "success", solidHeader= TRUE,
            
            tabBox(width = 700,
                   
              tags$h4("Under construction")
              
            )
          ) # close box
          
        ) # close fluid row
) # close tabit
