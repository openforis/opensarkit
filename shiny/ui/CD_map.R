#-----------------------------------------------------------------------------
# S1 Tab
tabItem(tabName = "ms_cd",
        fluidRow(
          #----------------------------------------------------------------------------------
          # Processing Panel 
          box(
            # Title                     
            title = "OST Change Detection", status = "success", solidHeader= TRUE,
            
            tabBox(width = 700,
                   
                   tags$h4("Under construction")
                   
            )
          ) # close box
          
        ) # close fluid row
) # close tabit
