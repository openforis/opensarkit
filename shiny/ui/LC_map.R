#-----------------------------------------------------------------------------
# S1 Tab
tabItem(tabName = "ms_lc_class",
        fluidRow(
          #----------------------------------------------------------------------------------
          # Processing Panel 
          box(
            # Title                     
            title = "OST Land Cover Classification", status = "success", solidHeader= TRUE,
            
            tabBox(width = 700,
                   
                   tags$h4("Under construction")
                   
            )
          ) # close box
          
        ) # close fluid row
) # close tabit
