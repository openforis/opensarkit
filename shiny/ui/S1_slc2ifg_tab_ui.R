#-----------------------------------------------------------------------------
# S1 Tab
tabItem(tabName = "s1_slc2ifg",
        fluidRow(
          #----------------------------------------------------------------------------------
          # Processing Panel Sentinel-1
          box(
            # Title                     
            title = "Sentinel-1 SLC to Interferogram processor", status = "success", solidHeader= TRUE,
            
            tabBox(width = 700,
                   
              tags$h4("Under construction")
              
            )
          ) # close box
          
        ) # close fluid row
) # close tabit
