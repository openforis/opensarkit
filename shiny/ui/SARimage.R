tabItem(tabName = "SARimage",
        "Image interpretation of SAR imagery",
        fluidRow(
          box(
            # Title                     
            title = "Processing Panel", status = "success", solidHeader= TRUE,
            tags$h4("SAR image interpretation"),
            hr(),
            tags$b("Under construction"),br()
        )
        ) # close box
) # close tabItem